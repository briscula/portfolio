# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema\#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Copy this file to .env and replace with your actual values
# Never commit .env files to version control

# Database connection string
DATABASE_URL="postgresql://username:password@hostname:5432/database_name?schema=public"

# Auth0 Configuration
AUTH0_DOMAIN="your-tenant.auth0.com"
AUTH0_AUDIENCE="https://your-api-identifier.com"

# Application Configuration
NODE_ENV="development"  # development | production | test
PORT=3000

# Logging Configuration
# LOG_LEVEL controls what logs are shown:
# - error: Only errors
# - warn: Errors and warnings  
# - log: Errors, warnings, and general logs (default for production)
# - debug: All above + debug info (default for development)
# - verbose: All logs including verbose details
LOG_LEVEL="debug"  # error | warn | log | debug | verbose

# Add other environment variables here as needed
# Legacy JWT (remove after Auth0 migration)
# JWT_SECRET=your_jwt_secret_here
